# Local API. On Netlify, set VITE_API_URL to your Railway (or other) public API URL.
VITE_API_URL=http://localhost:3000

# Supabase Auth (Phase 11) — from Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=
